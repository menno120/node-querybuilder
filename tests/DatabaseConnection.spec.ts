import 'mocha';
import { expect } from 'chai';
import DatabaseConnection from '../src/classes/DatabaseConnection';

describe('DatabaseConnection', () => {
	describe('constructor', () => {
		// @todo
	});
});
